{
  "name": "brute-force-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm dev",
    "build": "pnpm build",
    "start": "pnpm start",
    "lint": "pnpm lint",
    "test": "pnpm test"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
