/**
 * Application-wide constants
 */

/** Maximum Computing Power a user can have */
export const CP_MAX = 50;

/** CP refill rate in milliseconds (1 per minute) */
export const CP_REFILL_RATE_MS = 60000;

/** Timeout in seconds for winner to submit hint */
export const HINT_TIMEOUT_SECONDS = 180;

/** Maximum number of attempts to display in feeds */
export const ATTEMPTS_DISPLAY_LIMIT = 50;

/** Query stale time in milliseconds */
export const QUERY_STALE_TIME_MS = 60000;

/** Block refetch interval in milliseconds */
export const BLOCK_REFETCH_INTERVAL_MS = 5000;

/** CP refetch interval in milliseconds */
export const CP_REFETCH_INTERVAL_MS = 60000;
